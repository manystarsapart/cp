#include <bits/stdc++.h>
using namespace std;

int main() {
    long long x;
    cin >> x;
    if (x == 1) {
        cout << "Hello World";
    } else if (x == 2) {
        long long a;
        long long b;
        cin >> a >> b;
        cout << a*b;
    } 
}
