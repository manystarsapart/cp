#include <bits/stdc++.h> 
using namespace std;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); // fast i/o
    long long N; cin >> N;
    vector<int> va, vb = {};
    
    for (long long i=0; i<N; i++) {
        int addA, addB; cin >> addA >> addB;
        va.push_back(addA);
        vb.push_back(addB);
        
    }



}