#include <bits/stdc++.h>
using namespace std;

long long add(long long a, long long b) {
    long long c = a + b;
    return c;
}

int main() {
    long long x;
    long long y;
    cin >> x >> y; 
    cout << add(x,y);
}


